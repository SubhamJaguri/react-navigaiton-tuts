import React from 'react';
import { View, Text } from 'react-native';
import { styles } from '../../styles/styles';
const Tab3 = () => {
  return (
    <View tyle={styles.center}>
      <Text tyle={styles.title}> Tab3</Text>
    </View>
  );
};

export default Tab3;
