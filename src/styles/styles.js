import { Stylesheet } from 'react-native';

export const styles = Stylesheet.creat({
  center: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: {
    fontSize: 36,
    marginBottom: 16,
  },
});
